# 💰 Budget Tracker (Enterprise Edition)

A robust, secure, and scalable Personal Finance Management System built with **Spring Boot** and **PostgreSQL**. This project demonstrates modern backend architecture, secure authentication patterns, and financial data processing.

## 🚀 Features

* **Security First:** JWT-ready architecture with Spring Security & BCrypt password hashing.
* **User Management:** Registration, Login, and Email Verification (SMTP).
* **Financial Tracking:** CRUD operations for Transactions (Income/Expense).
* **Automation:** Scheduled jobs (Cron) for recurring payments (e.g., Rent, Netflix).
* **Data Analysis:** Advanced filtering, searching, and Excel reporting (Apache POI).
* **Market Data:** Real-time integration with ExchangeRate-API (Fiat) and Binance API (Crypto).
* **Global Standards:** English code base, comprehensive Javadoc, and professional error handling.

## 🛠️ Tech Stack

* **Language:** Java 21 (LTS)
* **Framework:** Spring Boot 3.4
* **Database:** PostgreSQL
* **ORM:** Hibernate / JPA
* **Build Tool:** Maven
* **External Libs:** Apache POI (Excel), Lombok, JavaMailSender

## ⚙️ Getting Started

### Prerequisites
* Java 21 JDK
* PostgreSQL
* Maven

### Installation
1.  Clone the repo:
    ```bash
    git clone [https://github.com/your-username/budget-tracker-backend.git](https://github.com/your-username/budget-tracker-backend.git)
    ```
2.  Configure database in `src/main/resources/application.properties`.
3.  Run the application:
    ```bash
    mvn spring-boot:run
    ```

## 📧 Contact
**Ibo** - [Your LinkedIn Profile]